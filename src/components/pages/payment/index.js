import React, { useState } from 'react';
import FlipClock from '../../common/flip-clock';

const PaymentPage = () => {
    return (
        <div>
            <FlipClock />
        </div>
    );
};

export default PaymentPage;